import React from 'react';
import {Form, Button, Col, fieldset, Row, Navbar} from 'react-bootstrap';
import Menu from './../Menu';
import {CustomButton, CustomButton2} from '../../styled';

function Header() {
	return(
		<>
	        <Menu />
		</>
	);
}

export default Header;